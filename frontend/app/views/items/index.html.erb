<h1 class="text-2xl font-bold mb-4">Items List</h1>

<ul class="space-y-2">
  <% @items.each do |item| %>
    <li class="p-2 border rounded hover:bg-gray-100">
      <a href="<%= item_path(item['id']) %>" class="text-blue-600"><%= item['name'] %></a>
    </li>
  <% end %>
</ul>
